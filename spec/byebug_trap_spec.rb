require 'spec_helper'

RSpec.describe ByebugTrap do
  it 'has a version number' do
    expect(ByebugTrap::VERSION).not_to be nil
  end

  xit 'does something useful' do
    expect(false).to eq(true)
  end
end
